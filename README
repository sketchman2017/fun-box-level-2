https://dl.fun-box.ru/qt-js.pdf

1. cd funbox_part_2
2. python
>>>help() 
help> modules
help> CGIHTTPServer

You see at the top of file: /home/your_user/CGIHTTPServer.py

Ctrl-Z

3. python /home/your_user/CGIHTTPServer.py

It is a server.

4. Then go to http://127.0.0.1:8000/ in browser.

5. Tests (Ubuntu): 

5.1 e2e:

sudo apt-get install nodejs
sudo apt-get install npm
sudo npm install -g karma
sudo npm install -g protractor
sudo npm install -g jasmine

webdriver-manager update
webdriver-manager start
protractor tests/conf.js

5.2 Unit:

sudo npm install -g karma

sudo npm install -g karma-jasmine
sudo npm install -g jasmine-core
sudo npm install -g karma@*

sudo npm install jasmine-core@*

sudo npm install -g karma-junit-reporter
sudo npm install -g karma@
sudo npm install karma-chrome-launcher -g
sudo npm install karma-firefox-launcher -g
sudo npm install karma-opera-launcher -g
sudo npm install karma-ie-launcher -g

(Конф. файл создан командой karma init karma.conf.js)

